# Copy this file to .env and fill in real values.
# .env is gitignored; this file is committed so others know which keys exist.

# Base URL of the site being parsed (decided in Phase 1)
BASE_URL=

# Seconds to wait between requests (Phase 3)
RATE_LIMIT_DELAY=1
